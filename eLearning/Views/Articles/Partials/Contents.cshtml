@model ContentsVM

<h1>@Model.ArticleDisplay</h1>

<ul>
    @foreach (var chapter in Model.Contents)
    {
        <li>
            <a href="@Url.Action("Chapter", "Articles", new { Id = Model.ArticleUid, ChapterId = chapter.Uid })">@chapter.Display</a>
        </li>
    }
</ul>